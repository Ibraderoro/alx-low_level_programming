#include "main.h"
/**
 * mul - This function that multiplies two integers
 * @a: is an integer
 * @b: is an ineger
 * Return: an integer
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
