#include "main.h"

/**
 * _isupper - This function that checks for uppercase character
 * @c: This is a character to be print
 * Return: 1 if c is uppercase or 0 if not
 */
int _isupper(int c)
{
if (c >= A && c <= Z)
{
return (1);
}
return (0);
}
